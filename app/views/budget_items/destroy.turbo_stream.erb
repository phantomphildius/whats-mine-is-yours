<%= turbo_stream.update "total_budget" do %>
  <%= render "budgets/total", budget: @budget %>
<% end %>
<%= turbo_stream.update "budget_breakdown", partial: 'budgets/breakdown', locals: { budget: @budget } %>

<%= turbo_stream.remove @budget_item %>
