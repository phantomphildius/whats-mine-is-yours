<h2><%= @budget.title %></h2>
<%
=begin%>
  Use stream.replace to update this number  
<%
=end%>
<h3><%= humanized_money_with_symbol(@budget.total) %></h3>

<% if @budget.items.any? %>
<table>
  <thead>
    <tr>
      <th>Category</th>
      <th>Amount</th>
    </tr>
  </thead>
  <%
=begin%>
 <turbo_stream action="append" target="budget_items">
    <template> 
<%
=end%>
      <tbody id="budget_items">
        <%= render @budget.items.persisted %>
      </tbody>
    <%
=begin%>
 </template>
   </turbo_stream> 
<%
=end%>
</table>
<% else %>
  <div>Add a budget item</div>
<% end %>
<%= render 'budget_items/form', budget_item: @new_budget_item %>
